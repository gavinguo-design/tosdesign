# 桌面卡片2（调试）

This is Lingxi's isolated debug module for smart suggestion card experiments.

Rules:

- Do not edit `../desktop-card/` when working on this module.
- Do not change the existing homepage entry that points to `desktop-card/index.html`.
- All smart suggestion card experiments should happen in `desktop-card-2/` or in the Lingxi skill directory.
- If a change needs shared API or backend behavior, create a separate route or explicit debug flag before testing.
